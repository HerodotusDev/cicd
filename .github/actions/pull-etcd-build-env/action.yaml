name: "Pull ETCD Build Env"
description: "Fetch a .env file from ETCD and write it to /tmp so it survives a later checkout step."

inputs:
  etcd_host:
    description: "ETCD endpoint"
    required: false
    default: http://etcd.etcd.svc.cluster.local:2379
  etcd_user:
    description: "ETCD username"
    required: true
  etcd_password:
    description: "ETCD password"
    required: true
  etcd_root:
    description: "ETCD root path"
    required: true
  app_name:
    description: "Application name (etcd key segment)"
    required: true
  k8s_env:
    description: "Kubernetes environment (stg / prod)"
    required: true
  namespace:
    description: "Kubernetes namespace (used to resolve etcd host)"
    required: true

outputs:
  env_file:
    description: "Absolute path to the written .env file (empty if not found)"
    value: ${{ steps.extract.outputs.path }}
  found:
    description: "Whether the etcd key existed"
    value: ${{ steps.extract.outputs.found }}

runs:
  using: "composite"
  steps:
    - name: Resolve consumer overrides
      id: overrides
      uses: HerodotusDev/cicd/.github/actions/resolve-overrides@main
      with:
        namespace: ${{ inputs.namespace }}

    - name: Resolve ETCD host
      id: resolve
      shell: bash
      env:
        OVERRIDE_HOST: ${{ steps.overrides.outputs.etcd_host }}
        OVERRIDE_NET: ${{ steps.overrides.outputs.etcd_docker_network }}
        OVERRIDE_USER: ${{ steps.overrides.outputs.etcd_user }}
        OVERRIDE_PASSWORD_ENV: ${{ steps.overrides.outputs.etcd_password_env }}
      run: |
        if [ -n "$OVERRIDE_HOST" ]; then
          echo "host=$OVERRIDE_HOST" >> "$GITHUB_OUTPUT"
          echo "docker_network=${OVERRIDE_NET:-host}" >> "$GITHUB_OUTPUT"
        else
          echo "host=${{ inputs.etcd_host }}" >> "$GITHUB_OUTPUT"
          echo "docker_network=bridge" >> "$GITHUB_OUTPUT"
        fi
        if [ -n "$OVERRIDE_USER" ]; then
          echo "etcd_user=$OVERRIDE_USER" >> "$GITHUB_OUTPUT"
        else
          echo "etcd_user=${{ inputs.etcd_user }}" >> "$GITHUB_OUTPUT"
        fi
        if [ -n "$OVERRIDE_PASSWORD_ENV" ]; then
          echo "etcd_password=${!OVERRIDE_PASSWORD_ENV}" >> "$GITHUB_OUTPUT"
        else
          echo "etcd_password=${{ inputs.etcd_password }}" >> "$GITHUB_OUTPUT"
        fi

    - name: Pull .env from ETCD
      id: pull
      continue-on-error: true
      shell: bash
      run: |
        set -e
        ETCD_KEY="/${{ inputs.etcd_root }}/${{ inputs.k8s_env }}/${{ inputs.app_name }}/.env"
        OUTPUT_DIR="${{ github.workspace }}/build-env-output"
        mkdir -p "${OUTPUT_DIR}"

        echo "Fetching etcd key: ${ETCD_KEY}"

        NETWORK_FLAG=""
        if [ "${{ steps.resolve.outputs.docker_network }}" = "host" ]; then
          NETWORK_FLAG="--network host"
        fi

        docker run --rm $NETWORK_FLAG \
          -e ETCD_HOST="${{ steps.resolve.outputs.host }}" \
          -e ETCD_USER="${{ steps.resolve.outputs.etcd_user }}" \
          -e ETCD_PASSWORD="${{ steps.resolve.outputs.etcd_password }}" \
          -e ETCD_KEY="${ETCD_KEY}" \
          -e APP_NAME="${{ inputs.app_name }}" \
          -e NAMESPACE="${{ inputs.namespace }}" \
          -v "${OUTPUT_DIR}:/output" \
          dataprocessor/etcd-pull:0.2 || true

        if [ -s "${OUTPUT_DIR}/secret.yaml" ]; then
          echo "no_key=false" >> "$GITHUB_OUTPUT"
          echo "output_dir=${OUTPUT_DIR}" >> "$GITHUB_OUTPUT"
        else
          echo "no_key=true" >> "$GITHUB_OUTPUT"
          echo "::warning::No .env found at etcd key ${ETCD_KEY}"
        fi

    - name: Extract raw .env from secret
      id: extract
      if: steps.pull.outputs.no_key == 'false'
      shell: bash
      run: |
        set -euo pipefail
        OUTPUT_DIR="${{ steps.pull.outputs.output_dir }}"
        OUT="/tmp/etcd-build-env"

        sudo chown -R "$(whoami):$(whoami)" "${OUTPUT_DIR}"

        # Secret data has one key per env var, each base64-encoded.
        # Reconstruct a .env file: decode each value and write KEY=VALUE lines.
        in_data=false
        while IFS= read -r line; do
          if [[ "$line" == "data:" ]]; then
            in_data=true
            continue
          fi
          if $in_data; then
            if [[ "$line" =~ ^[[:space:]]+([^:]+):[[:space:]]+(.+)$ ]]; then
              key="${BASH_REMATCH[1]}"
              val="${BASH_REMATCH[2]}"
              decoded=$(echo "$val" | base64 -d 2>/dev/null || echo "$val")
              echo "${key}=${decoded}" >> "${OUT}"
            elif [[ ! "$line" =~ ^[[:space:]] ]]; then
              break
            fi
          fi
        done < "${OUTPUT_DIR}/secret.yaml"

        if [ -s "${OUT}" ]; then
          echo "found=true" >> "$GITHUB_OUTPUT"
          echo "path=${OUT}" >> "$GITHUB_OUTPUT"
          echo "Wrote $(wc -l < "${OUT}") lines to ${OUT}"
        else
          echo "found=false" >> "$GITHUB_OUTPUT"
          echo "path=" >> "$GITHUB_OUTPUT"
          echo "::warning::Failed to extract env vars from secret.yaml"
        fi

        rm -rf "${OUTPUT_DIR}"
